package com.example.VotingProject.in.voter.controller;

public @interface updateMapping { //indicates that the class is a controller where every method returns a domain object instead of a view
//maps HTTP PUT requests to /api/emp/update/{empId} to this method
}
